Tres metodos importantes em DICIONARIOS 

    x = dicionario.items() = para obter todos os items do dicionario
    x = dicionario.keys() = devolve as chaves do dicionario
    x = dicionario.values() = devolve os valores do dicionario 

    x = dicionario.get()  = busca info nos dicionarios mais nao altera ( busca uma copia )
    x = dicionario.pop()  = busca  a info no dicionario mais apaga o que estva la no dicionario

    copia_x = dicionario.copy()